[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: hits real Sleeper/KTC/FantasyPros APIs over the network; run day-to-day with `pytest -m "not integration"`, or explicitly with `pytest -m integration`
